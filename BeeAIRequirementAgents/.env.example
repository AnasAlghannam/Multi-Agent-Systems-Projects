# Copy to `.env` and fill in. `.env` is git-ignored.

# Groq API key — required. Free at https://console.groq.com/keys
GROQ_API_KEY=

# Optional: swap provider/model. BeeAI addresses models as "provider:model"
# and supports groq, openai, watsonx, ollama, anthropic, gemini and more.
# BEEAI_PROVIDER=groq
# BEEAI_MODEL=llama-3.3-70b-versatile
