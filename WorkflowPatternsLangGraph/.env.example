# Copy to `.env` and fill in. `.env` is git-ignored.

# Groq API key — required. Free at https://console.groq.com/keys
GROQ_API_KEY=

# Optional: override the model used by all workflows.
# WORKFLOW_MODEL=llama-3.3-70b-versatile

# Optional: Gradio port (default 7860).
# APP_PORT=7860
